# Environment variables for OnTime Dataform Project

# Google Cloud Storage bucket for storing the aviation data
# Format: gs://your-bucket-name/optional-path
GCS_BUCKET=gs://your-bucket-name/ontime-data

# Dataform initialization command (already in .env)
# init_command="dataform init . personal-use-461616 asia-southeast1"

# Example for setting GCS_BUCKET when running the script:
# export GCS_BUCKET=gs://my-ontime-data-bucket/aviation-data
# ./includes/load_data_to_gcs.zsh
